# Story 5.3: 통계 화면 구현

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a 학습자,
I want 통계 화면에서 전체 학습 진행 결과를 검토할 수 있기를,
so that 학습 루틴이 유지되고 있다는 성취감과 동기부여를 얻는다.

## Acceptance Criteria

1. **Given** 통계 화면(`/stats`)에 접근했을 때 **When** 통계 데이터가 표시되면 **Then** 주제별 누적 학습 시간이 표시된다 (FR20)
2. **And** 전체 학습 진행 결과가 요약되어 표시된다 (FR21)
3. **And** 오늘 기준과 주간 기준의 학습 상태가 구분되어 표시된다 (FR22)
4. **And** 화면 전환 시 즉각적으로 반응한다 (NFR2)
5. **And** 키보드로 탐색 가능하다 (UX-DR11)
6. **And** 데이터 없음 상태에서는 빈 상태 안내가 표시된다 (UX-DR8)
7. **And** 진행률은 색상 외 수치/레이블로도 표현된다 (UX-DR11)

## Tasks / Subtasks

- [x] Task 1: 통계 서비스 구현 (AC: #1, #2, #3, #4)
  - [x] 1.1 `src/features/stats/statistics-service.ts` 생성
  - [x] 1.2 `StatsPageData` 타입 정의 — today, weekly, byTopic 데이터 통합
  - [x] 1.3 `loadStatsPageData()` 함수 구현 — `getTodayStudySummary`, `getWeeklyStudySummary`, `getStudyByTopic` 병렬 호출
  - [x] 1.4 `hasData` 플래그 계산: 세션 기록이 하나라도 있는지

- [x] Task 2: 통계 훅 구현 (AC: #4)
  - [x] 2.1 `src/features/stats/hooks/useStatistics.ts` 생성
  - [x] 2.2 `useState` + `useEffect` + `useCallback` 패턴 (기존 `useDashboardData` 참고)
  - [x] 2.3 `isLoading`, `error`, `data`, `refetch` 반환

- [x] Task 3: StatsSummaryCard 컴포넌트 구현 (AC: #2, #3, #7)
  - [x] 3.1 `src/features/stats/components/StatsSummaryCard.tsx` 생성
  - [x] 3.2 오늘 누적 시간/세션 수, 이번 주 누적 시간/세션 수 표시
  - [x] 3.3 `default.svg` 캐릭터 보조 표시 (`aria-hidden="true"` + 텍스트 동반)
  - [x] 3.4 시간 포맷: `formatMinutes()` 로컬 함수 (Story 5.2 패턴 동일)
  - [x] 3.5 `StatsSummaryCard.module.css` — `tokens.css` 변수만 사용

- [x] Task 4: TopicBreakdownList 컴포넌트 구현 (AC: #1, #5, #6, #7)
  - [x] 4.1 `src/features/stats/components/TopicBreakdownList.tsx` 생성
  - [x] 4.2 주제별 누적 시간 리스트 표시 — `TopicStudySummary[]` 데이터 매핑
  - [x] 4.3 각 주제 항목: 주제명, 누적 시간, 세션 수, 비율 바(전체 대비 비율)
  - [x] 4.4 비율은 `--color-primary` 기반 프로그레스바 + 퍼센트 수치 텍스트
  - [x] 4.5 데이터 없음 상태: `default.svg` + "아직 학습 기록이 없어요" 안내
  - [x] 4.6 리스트는 키보드 탐색 가능 (`tabIndex`, 적절한 `aria-label`)
  - [x] 4.7 `TopicBreakdownList.module.css` — `tokens.css` 변수만 사용

- [x] Task 5: StatsRoute 교체 (AC: #1, #2, #3, #4, #5, #6, #7)
  - [x] 5.1 `src/app/routes/StatsRoute.tsx`에서 `useStatistics` 훅 호출
  - [x] 5.2 기존 플레이스홀더 콘텐츠 전체 교체
  - [x] 5.3 `<StatsSummaryCard>` + `<TopicBreakdownList>` 렌더링
  - [x] 5.4 로딩 중 스켈레톤/최소 높이 유지 (레이아웃 시프트 방지)
  - [x] 5.5 에러 상태 처리: 사용자 친화적 메시지 표시
  - [x] 5.6 `StatsRoute.module.css` 생성 — 페이지 레이아웃 스타일

- [x] Task 6: 유닛 테스트 (AC: #1, #2, #3)
  - [x] 6.1 `tests/statistics-service.test.mjs` 생성
  - [x] 6.2 `loadStatsPageData()` 정상 동작 테스트
  - [x] 6.3 에러 전파 테스트
  - [x] 6.4 `hasData` 계산 테스트

- [x] Task 7: 빌드 검증 (AC: #4)
  - [x] 7.1 `npx tsc --noEmit` 통과
  - [x] 7.2 `npm run build` 성공
  - [x] 7.3 기존 테스트 회귀 없음

## Dev Notes

### 핵심 기술 요구사항

| 항목 | 상세 |
|------|------|
| 의존 Story | Story 5.1 (통계 도메인 + adapter — **완료됨**) |
| 의존 Story | Story 5.2 (홈 대시보드 Summary Card — **완료됨**, `formatMinutes` 패턴, `StudyStatusSummaryCard` 참고) |
| 도메인 경로 | `src/domain/statistics/` — **이미 구현 완료** (statistics.ts, statistics-repository.ts, statistics-mappers.ts) |
| Adapter 경로 | `src/platform/browser/statistics-adapter.ts` — **이미 구현 완료** |
| Feature 경로 | `src/features/stats/` — **현재 비어 있음 (디렉터리 자체 없을 수 있음). 이 스토리에서 서비스, 훅, 컴포넌트 생성** |
| 상태 관리 | Zustand store 변경 없음. 훅에서 로컬 `useState`로 관리 |
| 라우트 변경 | `StatsRoute.tsx` 플레이스홀더 → 실제 통계 화면으로 교체 |

### 아키텍처 패턴 — 반드시 따를 것

#### 1. Statistics Service 패턴

기존 `dashboard-service.ts` 패턴을 따른다. adapter 함수를 조합해 통합 데이터를 반환한다:

```typescript
// src/features/stats/statistics-service.ts
import { getTodayStudySummary, getWeeklyStudySummary, getStudyByTopic } from '../../platform/browser/statistics-adapter';
import { getTodayStartAtMs, getWeekStartAtMs } from '../../shared/lib/dates';
import { ok, err, type Result } from '../../shared/lib/result';
import type { TodayStudySummary, WeeklyStudySummary, TopicStudySummary } from '../../domain/statistics/statistics';

export interface StatsPageData {
  today: TodayStudySummary;           // { totalMinutes, sessionCount }
  weekly: WeeklyStudySummary;         // { totalMinutes, sessionCount }
  byTopic: TopicStudySummary[];       // [{ topicId, topicName, totalMinutes, sessionCount }]
  totalMinutesAllTime: number;        // 전체 기간 누적 분 (byTopic 합산)
  totalSessionsAllTime: number;       // 전체 기간 세션 수 (byTopic 합산)
  hasData: boolean;                   // 세션 기록이 하나라도 있는지
}

export async function loadStatsPageData(): Promise<Result<StatsPageData>> {
  try {
    const todayStartMs = getTodayStartAtMs();
    const weekStartMs = getWeekStartAtMs();

    const [todayResult, weeklyResult, topicResult] = await Promise.all([
      getTodayStudySummary(todayStartMs),
      getWeeklyStudySummary(weekStartMs),
      getStudyByTopic(),
    ]);

    if (!todayResult.ok) return todayResult;
    if (!weeklyResult.ok) return weeklyResult;
    if (!topicResult.ok) return topicResult;

    const byTopic = topicResult.data;
    const totalMinutesAllTime = byTopic.reduce((sum, t) => sum + t.totalMinutes, 0);
    const totalSessionsAllTime = byTopic.reduce((sum, t) => sum + t.sessionCount, 0);

    const hasData = todayResult.data.sessionCount > 0
      || weeklyResult.data.sessionCount > 0
      || byTopic.length > 0;

    return ok({
      today: todayResult.data,
      weekly: weeklyResult.data,
      byTopic,
      totalMinutesAllTime,
      totalSessionsAllTime,
      hasData,
    });
  } catch (error) {
    return err(
      'UNEXPECTED_ERROR',
      `통계 데이터 로드 중 오류: ${error instanceof Error ? error.message : String(error)}`,
    );
  }
}
```

#### 2. 훅 패턴 (기존 `useDashboardData` 동일)

```typescript
// src/features/stats/hooks/useStatistics.ts
import { useState, useEffect, useCallback } from 'react';
import { loadStatsPageData, type StatsPageData } from '../statistics-service';

export function useStatistics() {
  const [data, setData] = useState<StatsPageData | null>(null);
  const [isLoading, setIsLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const fetchData = useCallback(async () => {
    setIsLoading(true);
    setError(null);
    const result = await loadStatsPageData();
    if (result.ok) {
      setData(result.data);
    } else {
      setError(result.message);
    }
    setIsLoading(false);
  }, []);

  useEffect(() => {
    fetchData();
  }, [fetchData]);

  return { data, isLoading, error, refetch: fetchData };
}
```

#### 3. 컴포넌트 구조

| 컴포넌트 | 역할 | Props |
|---------|--------|-------|
| `StatsSummaryCard` | 오늘/주간 요약 표시 | `today: TodayStudySummary`, `weekly: WeeklyStudySummary` |
| `TopicBreakdownList` | 주제별 누적 시간 리스트 | `topics: TopicStudySummary[]`, `totalMinutes: number` |

#### 4. 화면 상태 매핑

| 상태 | 조건 | 시각적 표현 |
|------|------|-------------|
| 로딩 | `isLoading === true` | 스켈레톤 또는 최소 높이 카드 → `aria-busy="true"` |
| 에러 | `error !== null` | "데이터를 불러올 수 없습니다" 메시지 |
| 데이터 없음 | `hasData === false` | `default.svg` + "아직 학습 기록이 없어요" 안내 |
| 데이터 있음 | `hasData === true` | Summary Card + Topic Breakdown List |

#### 5. 시간 포맷 유틸

Story 5.2 `StudyStatusSummaryCard.tsx`에서 사용한 동일한 `formatMinutes` 로컬 함수를 **각 컴포넌트에서 동일하게** 사용한다. 2개 이상 feature에서 재사용 시 `shared/lib/formatters.ts`로 추출 고려하지만, 현재는 로컬 함수로 유지:

```typescript
function formatMinutes(minutes: number): string {
  if (minutes < 60) return `${minutes}m`;
  const h = Math.floor(minutes / 60);
  const m = minutes % 60;
  return m > 0 ? `${h}h ${m}m` : `${h}h`;
}
```

⚠️ **Story 5.2에서 동일 함수가 `StudyStatusSummaryCard.tsx`에 로컬 정의되어 있음**. 이 스토리에서도 `stats` feature 컴포넌트 내에 동일하게 로컬 정의. **shared로 추출하지 않는다** (아직 3개 미만 사용처).

#### 6. 주제별 비율 계산

`TopicBreakdownList`에서 각 주제의 학습 시간 비율을 표시:

```typescript
// 전체 누적 시간 대비 해당 주제 비율
const percent = totalMinutes > 0
  ? Math.round((topic.totalMinutes / totalMinutes) * 100)
  : 0;
```

#### 7. 캐릭터 자산 사용 패턴

```typescript
import defaultCharacter from '../../../assets/characters/default.svg';
// <img src={defaultCharacter} alt="" aria-hidden="true" />
// 반드시 텍스트 동반 — SVG만으로 의미 전달 금지 (UX-DR4, UX-DR11)
```

#### 8. StatsRoute 통합 시 주의사항

```tsx
// src/app/routes/StatsRoute.tsx 교체
import { useStatistics } from '../../features/stats/hooks/useStatistics';
import { StatsSummaryCard } from '../../features/stats/components/StatsSummaryCard';
import { TopicBreakdownList } from '../../features/stats/components/TopicBreakdownList';
import styles from './StatsRoute.module.css';

export function StatsRoute() {
  const { data, isLoading, error } = useStatistics();

  if (error) {
    return (
      <section className={styles.statsPage}>
        <h1>통계</h1>
        <p role="alert" className={styles.errorText}>데이터를 불러올 수 없습니다.</p>
      </section>
    );
  }

  if (isLoading || !data) {
    return (
      <section className={styles.statsPage}>
        <h1>통계</h1>
        <div className={styles.skeleton} aria-busy="true" aria-label="로딩 중" />
      </section>
    );
  }

  if (!data.hasData) {
    return (
      <section className={styles.statsPage}>
        <h1>통계</h1>
        <div className={styles.emptyState}>
          <img src={defaultCharacter} alt="" aria-hidden="true" />
          <p>아직 학습 기록이 없어요</p>
          <p>학습 세션을 완료하면 여기에 통계가 표시됩니다.</p>
        </div>
      </section>
    );
  }

  return (
    <section className={styles.statsPage}>
      <h1>통계</h1>
      <StatsSummaryCard today={data.today} weekly={data.weekly} />
      <TopicBreakdownList topics={data.byTopic} totalMinutes={data.totalMinutesAllTime} />
    </section>
  );
}
```

### 이 스토리에서 생성/수정하는 파일

```text
[NEW] src/features/stats/statistics-service.ts                   — 통계 페이지 데이터 로드 서비스
[NEW] src/features/stats/hooks/useStatistics.ts                  — 통계 데이터 훅
[NEW] src/features/stats/components/StatsSummaryCard.tsx          — 오늘/주간 요약 카드
[NEW] src/features/stats/components/StatsSummaryCard.module.css   — 요약 카드 스타일
[NEW] src/features/stats/components/TopicBreakdownList.tsx        — 주제별 누적 시간 리스트
[NEW] src/features/stats/components/TopicBreakdownList.module.css — 주제별 리스트 스타일
[MODIFY] src/app/routes/StatsRoute.tsx                           — 플레이스홀더 → 실제 통계 화면 교체
[NEW] src/app/routes/StatsRoute.module.css                       — 통계 페이지 레이아웃 스타일
[NEW] tests/statistics-service.test.mjs                          — 통계 서비스 유닛 테스트
```

### 명시적으로 수정하지 말아야 할 영역

- `src/domain/statistics/` — Story 5.1에서 완성됨. 수정 불필요
- `src/platform/browser/statistics-adapter.ts` — 이미 완성. 재사용만
- `src/platform/browser/in-memory-statistics-adapter.ts` — 수정 불필요
- `src/features/dashboard/` — Story 5.2에서 완성됨. 수정 불필요
- `src/features/goals/` — 기존 코드 유지. 이 스토리에서 목표 데이터 사용 안함
- `src/features/session/` — 기존 코드 유지
- `src/shared/lib/result.ts`, `errors.ts`, `dates.ts` — 재사용만
- `src/shared/ui/` — 기존 Button 등 재사용만
- `src/db/migrations/` — 새 테이블/마이그레이션 없음
- `src/app/routes/HomeRoute.tsx` — 수정 불필요
- `src/features/records/` — Story 6.x 범위

### Anti-Patterns to Avoid

- ❌ 통계 쿼리를 직접 호출 — 반드시 `statistics-adapter.ts`의 위임 함수를 통해서만 접근
- ❌ SQL이나 Tauri plugin을 컴포넌트에서 직접 호출 — adapter/service 경계를 지킬 것
- ❌ `sessions` 테이블을 직접 쿼리하는 새 코드 — 이미 `statistics-repository`에 있음
- ❌ Zustand store에 통계 데이터 저장 — 훅에서 로컬 `useState`로 관리
- ❌ 새 CSS 변수 정의 — `tokens.css`의 기존 변수만 사용
- ❌ 캐릭터 SVG에 의미를 단독으로 부여 — 반드시 텍스트 동반, `aria-hidden="true"` on img
- ❌ 진행률/비율을 색상만으로 표현 — 퍼센트 수치 + 텍스트 레이블 반드시 포함 (WCAG AA)
- ❌ `dashboard-service.ts`에서 목표 데이터 import — 이 화면에서는 목표 데이터 불필요
- ❌ `WeeklyTrendPanel` 구현 — Architecture에 언급되었지만 MVP AC에 없음. 이 스토리 범위 외
- ❌ `formatMinutes`를 `shared/lib/formatters.ts`로 추출 — 현재 2개 feature만 사용, 로컬 유지
- ❌ `src/features/records/` 파일 생성 — Story 6.x 범위
- ❌ 복잡한 차트 라이브러리 도입 — CSS 기반 프로그레스바로 충분 (UX Anti-pattern: 통계 과잉 금지)

### CSS 스타일링 가이드

`tokens.css`에서 사용할 주요 변수:

| 용도 | 변수 |
|------|------|
| 카드 배경 | `--color-bg-elevated` |
| 카드 그림자 | `--shadow-md` |
| 카드 모서리 | `--radius-lg` |
| 주 텍스트 | `--color-text-primary` |
| 보조 텍스트 | `--color-text-secondary` |
| 3차 텍스트 | `--color-text-tertiary` |
| 프로그레스바 | `--color-primary` |
| 프로그레스바 트랙 | `--color-neutral-100` |
| 수치 강조 | `--font-size-2xl`, `--font-weight-bold` |
| 레이블 | `--font-size-sm`, `--font-weight-medium` |
| 간격 | `--spacing-4`, `--spacing-6`, `--spacing-8` |

### Previous Story Intelligence

**Story 5.1에서 확인된 패턴 (반드시 재사용):**

- `statistics-adapter.ts` — `getTodayStudySummary(todayStartMs)`, `getWeeklyStudySummary(weekStartAtMs)`, `getStudyByTopic()` 위임 함수 사용
- `dates.ts` — `getTodayStartAtMs()`, `getWeekStartAtMs()` 유틸 사용
- `statistics.ts` 타입 — `TodayStudySummary { totalMinutes, sessionCount }`, `WeeklyStudySummary { totalMinutes, sessionCount }`, `TopicStudySummary { topicId, topicName, totalMinutes, sessionCount }`
- 테스트: `tests/` 디렉터리, `.test.mjs`, `node --test` 러너

**Story 5.2에서 확인된 패턴:**

- `dashboard-service.ts` — `loadDashboardData()` 서비스 패턴, `Promise.all` 병렬 호출, `Result<T>` 반환
- `useDashboardData.ts` — `useState + useEffect + useCallback` 패턴, `{ data, isLoading, error, refetch }` 반환
- `StudyStatusSummaryCard.tsx` — `formatMinutes()` 로컬 함수, 상태별 렌더링 (에러/로딩/데이터없음/데이터있음), `aria-label` 사용, `data-state` 기반 CSS, `useNavigate` 라우팅
- `StudyStatusSummaryCard.module.css` — `tokens.css` 변수 사용, `@media (max-width: 600px)` 반응형
- AI Code Review 반영: 접근성 개선 (`div` → `button`), `aria-valuenow/aria-valuemax` 개선

**기존 코드 현재 상태:**

- `src/features/stats/` — **디렉터리 자체가 없을 수 있음**. 이 스토리에서 디렉터리 + 서비스/훅/컴포넌트 생성
- `src/app/routes/StatsRoute.tsx` — 플레이스홀더 (`<h1>통계</h1>` + `<p>통계 화면 (Story 5.x에서 구현 예정)</p>`). **전체 교체**
- `src/shared/ui/ProgressBar/` — 존재 여부 확인 필요. 없으면 `TopicBreakdownList` 내에 간단한 CSS 프로그레스바 구현

### Git Intelligence

최근 주요 커밋:
- `3febfad` (HEAD) — Story 5.1: 통계 도메인/repository/mapper/adapter 구현
- `9124e21` — Story 4.3: 주간 목표 진행 상태 표시
- `e9701b3` — Story 4.2: 목표 설정 UI
- `7f1d18f` — Story 4.1: 주간 목표 도메인

시사점:
- `statistics-adapter.ts` 3개 함수(`getTodayStudySummary`, `getWeeklyStudySummary`, `getStudyByTopic`)가 안정화됨 → statistics-service에서 직접 import
- `useDashboardData` 훅 패턴이 확립됨 → `useStatistics`도 동일 패턴
- CSS Modules + tokens.css 변수 사용 패턴이 모든 컴포넌트에서 일관됨
- Story 5.2에서 `formatMinutes` 로컬 함수 패턴이 확립됨

### Project Structure Notes

- Architecture 문서 `src/features/stats/` 구조 (line 666-674):
  - `StatsPage.tsx` — **이 스토리에서는 생성하지 않음** (StatsRoute가 페이지 역할. 기존 HomeRoute 패턴과 동일)
  - `components/StatsSummaryCard.tsx` — **이 스토리에서 생성**
  - `components/TopicBreakdownList.tsx` — **이 스토리에서 생성**
  - `components/WeeklyTrendPanel.tsx` — 이 스토리 범위 외 (AC에 포함되지 않음)
  - `hooks/useStatistics.ts` — **이 스토리에서 생성**
  - `statistics-service.ts` — **이 스토리에서 생성**

### 접근성 요구사항 (WCAG AA)

- 모든 수치 정보에 `aria-label` 제공
- 주제별 비율 프로그레스바: `role="progressbar"`, `aria-valuenow`, `aria-valuemin=0`, `aria-valuemax=100`, `aria-label`
- 빈 상태 캐릭터: `aria-hidden="true"` + 텍스트 동반
- 키보드 탐색: 주제 리스트의 각 항목이 포커스 가능
- 색상만으로 의미 전달 금지: 퍼센트 수치 텍스트 반드시 포함
- 로딩 상태: `aria-busy="true"`, `aria-label="로딩 중"`

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 5.3, line 570-584]
- [Source: _bmad-output/planning-artifacts/epics.md#Epic 5, line 532-534]
- [Source: _bmad-output/planning-artifacts/architecture.md#Project Structure, line 666-674 — features/stats 위치]
- [Source: _bmad-output/planning-artifacts/architecture.md#Frontend Architecture, line 248-289]
- [Source: _bmad-output/planning-artifacts/architecture.md#Implementation Patterns, line 384-489]
- [Source: _bmad-output/planning-artifacts/architecture.md#Feature Mapping, line 751-754]
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md#UX-DR11 접근성 WCAG AA, line 515-518]
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md#UX-DR8 빈 상태, line 483-486]
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md#Responsive Strategy, line 499-513]
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md#Anti-Patterns to Avoid, line 110-112 — 통계 과잉 금지]
- [Source: src/domain/statistics/statistics.ts — 통계 도메인 타입]
- [Source: src/platform/browser/statistics-adapter.ts — 런타임 분기 adapter]
- [Source: src/features/dashboard/dashboard-service.ts — 서비스 패턴 참고]
- [Source: src/features/dashboard/hooks/useDashboardData.ts — 훅 패턴 참고]
- [Source: src/features/dashboard/components/StudyStatusSummaryCard.tsx — 컴포넌트 패턴 참고]
- [Source: src/shared/lib/dates.ts — getTodayStartAtMs, getWeekStartAtMs]
- [Source: src/shared/lib/result.ts — Result 타입]
- [Source: src/shared/styles/tokens.css — 디자인 토큰 변수]
- [Source: src/app/routes/StatsRoute.tsx — 현재 플레이스홀더 코드]

## Dev Agent Record

### Agent Model Used

Antigravity (Claude)

### Debug Log References

- 모든 기존 13개 테스트 파일 회귀 없음 확인

### Completion Notes List

- ✅ Task 1: `statistics-service.ts` — `loadStatsPageData()` 구현, 통계 adapter 3개 함수 `Promise.all` 병렬 호출, `StatsPageData` 타입 정의
- ✅ Task 2: `useStatistics.ts` — `useDashboardData` 동일 패턴 (`useState+useEffect+useCallback`)
- ✅ Task 3: `StatsSummaryCard.tsx/.css` — 오늘/주간 학습 시간 + 세션 수 표시, 캐릭터 보조 요소, `aria-label` 접근성
- ✅ Task 4: `TopicBreakdownList.tsx/.css` — 주제별 누적 시간 리스트, CSS 프로그레스바 + 퍼센트 수치, 키보드 탐색 (`tabIndex`), 빈 상태 처리
- ✅ Task 5: `StatsRoute.tsx` — 플레이스홀더 전체 교체, 4개 상태 (에러/로딩/빈/데이터) 분기, `StatsRoute.module.css` 생성
- ✅ Task 6: `statistics-service.test.mjs` — 10개 테스트 (데이터 통합, hasData 3가지 경우, 비율 계산, 에러 구조, formatMinutes 검증)
- ✅ Task 7: `npx tsc --noEmit` 통과, `npm run build` 성공, 기존 13개 테스트 파일 전체 회귀 없음

### File List

- [NEW] `src/features/stats/statistics-service.ts`
- [NEW] `src/features/stats/hooks/useStatistics.ts`
- [NEW] `src/features/stats/components/StatsSummaryCard.tsx`
- [NEW] `src/features/stats/components/StatsSummaryCard.module.css`
- [NEW] `src/features/stats/components/TopicBreakdownList.tsx`
- [NEW] `src/features/stats/components/TopicBreakdownList.module.css`
- [MODIFY] `src/app/routes/StatsRoute.tsx`
- [NEW] `src/app/routes/StatsRoute.module.css`
- [NEW] `tests/statistics-service.test.mjs`

### Change Log

- 2026-03-20: Story 5.3 구현 완료 — 통계 서비스, 훅, StatsSummaryCard/TopicBreakdownList 컴포넌트/스타일, StatsRoute 통합, 유닛 테스트 10건 추가
- 2026-03-20: AI Code Review 반영 — TopicBreakdownList 접근성 개선 (`tabIndex` 제거, `aria-label` 단순화)
