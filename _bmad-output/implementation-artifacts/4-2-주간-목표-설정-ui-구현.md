# Story 4.2: 주간 목표 설정 UI 구현

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a 학습자,
I want 주제별 주간 학습 목표 시간을 설정할 수 있기를,
so that 이번 주에 얼마나 공부할지 명확한 기준이 생긴다.

## Acceptance Criteria

1. **Given** 주제가 존재할 때 **When** 목표 설정 다이얼로그를 열면 **Then** 주제별 주간 목표 시간(분)을 설정할 수 있다 (FR14)
2. **And** 0 이하의 값 입력 시 인라인 검증 오류가 표시된다
3. **And** 저장 후 목표가 SQLite에 보존된다 (FR33, NFR5)
4. **And** 이미 설정된 목표가 있으면 현재 값이 표시된다

## Tasks / Subtasks

- [x] Task 1: `getWeekStartAtMs()` 유틸리티 함수 구현 (AC: #1, #3)
  - [x] 1.1 `src/shared/lib/dates.ts` 파일 생성 — `getWeekStartAtMs(date?: Date): number` 함수 구현 (현재 주 월요일 00:00:00 UTC epoch ms 반환)
  - [x] 1.2 `tests/dates.test.mjs` — 월요일~일요일 각 요일에 대해 동일 weekStartAtMs 반환 검증, 경계값(자정 직전/직후) 검증

- [x] Task 2: Goal adapter 레이어 구현 (AC: #1, #3, #4)
  - [x] 2.1 `src/platform/browser/goal-adapter.ts` — `topic-adapter.ts` 패턴 그대로 따름. `GoalRepositoryAdapter` 인터페이스 정의. Tauri runtime → `weekly-goal-repository`, 브라우저 → `in-memory-goal-adapter`
  - [x] 2.2 `src/platform/browser/in-memory-goal-adapter.ts` — `in-memory-topic-adapter.ts` 패턴 따름. `createWeeklyGoal`, `updateWeeklyGoal`, `findByTopicAndWeek`, `findAllByWeek` in-memory 구현

- [x] Task 3: `goal-service.ts` 서비스 레이어 구현 (AC: #1, #3, #4)
  - [x] 3.1 `src/features/goals/goal-service.ts` — `topic-service.ts` 패턴 따름. `loadGoalForTopicThisWeek`, `saveGoal`, `loadAllGoalsThisWeek` 함수 구현
  - [x] 3.2 `loadGoalForTopicThisWeek(topicId)` — `getWeekStartAtMs()` 호출 후 `findByTopicAndWeek(topicId, weekStartAtMs)` 위임
  - [x] 3.3 `saveGoal(topicId, targetMinutes)` — 기존 목표 존재 여부 확인 후 `createWeeklyGoal` 또는 `updateWeeklyGoal` 분기

- [x] Task 4: `useWeeklyGoals` 훅 구현 (AC: #1, #4)
  - [x] 4.1 `src/features/goals/hooks/useWeeklyGoals.ts` — `useTopics.ts` 패턴 따름
  - [x] 4.2 `loadGoalForTopic(topicId)` / `saveGoalForTopic(topicId, targetMinutes)` 함수 expose
  - [x] 4.3 goals 상태를 `Map<topicId, WeeklyGoal | null>`로 관리

- [x] Task 5: `GoalSettingsDialog` 컴포넌트 구현 (AC: #1, #2, #4)
  - [x] 5.1 `src/features/goals/components/GoalSettingsDialog.tsx` — 다이얼로그 형태, 주제명 표시 + 목표 시간(분) 입력 필드
  - [x] 5.2 `src/features/goals/components/GoalSettingsDialog.module.css` — tokens.css 변수 사용, 다이얼로그 오버레이 + 중앙 정렬 카드
  - [x] 5.3 Zod 클라이언트 검증 — `UpdateWeeklyGoalSchema.safeParse({ targetMinutes })` 사용
  - [x] 5.4 이미 목표가 존재하면(`existingGoal` prop) 현재 `targetMinutes` 값을 입력 필드에 기본값으로 표시
  - [x] 5.5 저장 성공 시 다이얼로그 닫기, 실패 시 인라인 에러 메시지 표시
  - [x] 5.6 ESC 키로 다이얼로그 닫기, 오버레이 클릭으로 닫기
  - [x] 5.7 포커스 트랩: 다이얼로그 열릴 때 입력 필드에 자동 포커스, 닫힐 때 트리거 버튼으로 포커스 복원
  - [x] 5.8 `role="dialog"`, `aria-modal="true"`, `aria-labelledby`로 접근성 제공

- [x] Task 6: TopicCard에 목표 설정 버튼 통합 (AC: #1, #4)
  - [x] 6.1 TopicCard 기본 모드에 "목표 설정" 버튼 추가 (variant="text", size="small")
  - [x] 6.2 이미 목표가 설정된 주제에는 "목표: {N}분/주" 라벨 표시 + "수정" 버튼으로 전환
  - [x] 6.3 버튼 클릭 시 GoalSettingsDialog 열기, 목표 저장 후 TopicCard UI에 즉시 반영

- [x] Task 7: 단위 테스트 작성
  - [x] 7.1 `tests/dates.test.mjs` — weekStartAtMs 계산 검증 (Task 1.2에서 작성)
  - [x] 7.2 `tests/goal-service.test.mjs` — saveGoal의 create/update 분기 로직 검증

- [x] Task 8: 빌드 검증
  - [x] 8.1 `npx tsc --noEmit` 통과
  - [x] 8.2 `npm run build` 성공
  - [x] 8.3 기존 테스트 회귀 없음: `node --test tests/*.test.mjs`

## Dev Notes

### 핵심 기술 요구사항

| 항목 | 상세 |
|------|------|
| 의존 Story | Story 4.1 (WeeklyGoal 도메인 모델, repository, mappers, schema — **완료됨**) |
| 의존 Story | Story 1.2 (디자인 토큰, Button 컴포넌트 — **완료됨**) |
| 의존 Story | Story 2.2, 2.3 (TopicCard, TopicForm 패턴 — **완료됨**) |
| 도메인 경로 | `src/domain/goals/` — 이미 4개 파일 존재 (Story 4.1에서 생성). **수정 불필요** |
| Feature 경로 | `src/features/goals/` — 현재 `.gitkeep`만 존재. 이 스토리에서 핵심 UI 파일 생성 |
| Adapter 경로 | `src/platform/browser/` — goal-adapter.ts, in-memory-goal-adapter.ts 추가 |
| 유틸리티 경로 | `src/shared/lib/dates.ts` — 새 파일. `getWeekStartAtMs()` 함수 추가 |
| 스타일 패턴 | CSS Modules + tokens.css 변수 사용 (TopicCard.module.css, TopicForm.module.css 패턴 참고) |
| 상태 관리 | 이 스토리에서 Zustand store 변경 없음. `useWeeklyGoals` 훅에서 로컬 상태로 관리 |

### 아키텍처 패턴 — 반드시 따를 것

#### 1. Adapter 패턴 (`topic-adapter.ts` 그대로 따름)

```typescript
// src/platform/browser/goal-adapter.ts
import { isTauriRuntime } from '../runtime/runtime-detect';
import type { WeeklyGoal, CreateWeeklyGoalInput, UpdateWeeklyGoalInput } from '../../domain/goals/weekly-goal';
import type { Result } from '../../shared/lib/result';

export interface GoalRepositoryAdapter {
  createWeeklyGoal(input: CreateWeeklyGoalInput): Promise<Result<WeeklyGoal>>;
  updateWeeklyGoal(id: string, input: UpdateWeeklyGoalInput): Promise<Result<WeeklyGoal>>;
  findByTopicAndWeek(topicId: string, weekStartAtMs: number): Promise<Result<WeeklyGoal | null>>;
  findAllByWeek(weekStartAtMs: number): Promise<Result<WeeklyGoal[]>>;
}

let adapter: GoalRepositoryAdapter | null = null;

async function getAdapter(): Promise<GoalRepositoryAdapter> {
  if (adapter) return adapter;
  if (isTauriRuntime()) {
    const mod = await import('../../domain/goals/weekly-goal-repository');
    adapter = mod;
  } else {
    const mod = await import('./in-memory-goal-adapter');
    adapter = mod;
  }
  return adapter;
}

// 각 함수는 (await getAdapter()).functionName(...) 위임
```

#### 2. Service 패턴 (`topic-service.ts` 그대로 따름)

```typescript
// src/features/goals/goal-service.ts
import { createWeeklyGoal, updateWeeklyGoal, findByTopicAndWeek, findAllByWeek } from '../../platform/browser/goal-adapter';
import { getWeekStartAtMs } from '../../shared/lib/dates';
import type { WeeklyGoal } from '../../domain/goals/weekly-goal';
import type { Result } from '../../shared/lib/result';

/** 현재 주의 특정 주제 목표 조회 */
export async function loadGoalForTopicThisWeek(topicId: string): Promise<Result<WeeklyGoal | null>> {
  const weekStartAtMs = getWeekStartAtMs();
  return findByTopicAndWeek(topicId, weekStartAtMs);
}

/** 목표 저장 (신규이면 create, 기존이면 update) */
export async function saveGoal(topicId: string, targetMinutes: number): Promise<Result<WeeklyGoal>> {
  const weekStartAtMs = getWeekStartAtMs();
  const existing = await findByTopicAndWeek(topicId, weekStartAtMs);
  if (!existing.ok) return existing as Result<WeeklyGoal>;
  
  if (existing.data) {
    return updateWeeklyGoal(existing.data.id, { targetMinutes });
  }
  return createWeeklyGoal({ topicId, weekStartAtMs, targetMinutes });
}

/** 현재 주의 모든 목표 조회 */
export async function loadAllGoalsThisWeek(): Promise<Result<WeeklyGoal[]>> {
  const weekStartAtMs = getWeekStartAtMs();
  return findAllByWeek(weekStartAtMs);
}
```

#### 3. `getWeekStartAtMs()` 유틸리티 설계

```typescript
// src/shared/lib/dates.ts
/**
 * 주어진 날짜(기본: 현재)가 속한 주의 월요일 00:00:00 UTC epoch ms를 반환한다.
 * ISO 8601 기준: 주는 월요일에 시작한다.
 */
export function getWeekStartAtMs(date: Date = new Date()): number {
  const d = new Date(Date.UTC(date.getFullYear(), date.getMonth(), date.getDate()));
  const day = d.getUTCDay(); // 0=일, 1=월, ..., 6=토
  const diff = day === 0 ? -6 : 1 - day; // 월요일까지의 차이
  d.setUTCDate(d.getUTCDate() + diff);
  return d.getTime();
}
```

#### 4. GoalSettingsDialog 설계 가이드

```tsx
// src/features/goals/components/GoalSettingsDialog.tsx
interface GoalSettingsDialogProps {
  topicName: string;
  topicId: string;
  existingGoal: WeeklyGoal | null;
  isOpen: boolean;
  onClose: () => void;
  onSave: (topicId: string, targetMinutes: number) => Promise<Result<WeeklyGoal>>;
}
```

- **다이얼로그 구현**: `<dialog>` HTML 요소 또는 직접 오버레이+카드 패턴. 기존 프로젝트에 Dialog 공통 컴포넌트가 없으므로, **이 스토리 내에서 간단한 모달 오버레이를 직접 구현**한다. (공통 Dialog 컴포넌트 추출은 이후 필요 시 진행)
- TopicCard의 "아카이브 확인" 패턴(인라인 확인 UI)를 참고하되, 목표 설정은 전체 화면 오버레이 다이얼로그로 구현한다.
- **폼 패턴은 `TopicForm.tsx`를 참고**: Zod `safeParse` → 에러 표시 → `isSaving` 상태 → 결과 처리
- 입력 필드: `type="number"`, `min="1"`, label "주간 목표 (분)" 연결
- 현재 목표가 있으면 `defaultValue`로 표시, 없으면 빈 필드
- **에러 메시지**: `role="alert"`, `aria-describedby`로 연결
- 스타일: `tokens.css`의 `--color-*`, `--space-*`, `--radius-*`, `--font-*` 사용

#### 5. TopicCard 통합 가이드

`TopicCard.tsx`의 기본 보기 모드 (line 183-198)에 목표 관련 UI 추가:

```tsx
// 기본 보기 모드에 추가
<div className={styles.info}>
  <span className={styles.name}>{topic.name}</span>
  {weeklyGoal && (
    <span className={styles.goalBadge}>목표: {weeklyGoal.targetMinutes}분/주</span>
  )}
  <span className={styles.date}>{formatDate(topic.createdAtMs)}</span>
</div>
<div className={styles.actions}>
  <Button variant="text" size="small" onClick={handleOpenGoalDialog}>
    {weeklyGoal ? '목표 수정' : '목표 설정'}
  </Button>
  <Button variant="text" size="small" onClick={handleStartEdit}>수정</Button>
  <Button variant="destructive" size="small" onClick={handleArchiveClick}>정리</Button>
</div>
```

⚠️ **TopicCard에 `weeklyGoal` prop을 추가해야 함** — `TopicsPage`에서 `useWeeklyGoals` 훅으로 각 주제의 목표를 조회하여 전달.

### 이 스토리에서 생성/수정하는 파일

```text
[NEW] src/shared/lib/dates.ts                        — getWeekStartAtMs 유틸리티
[NEW] src/platform/browser/goal-adapter.ts            — Goal repository adapter (런타임 분기)
[NEW] src/platform/browser/in-memory-goal-adapter.ts  — 브라우저 QA용 in-memory goal adapter
[NEW] src/features/goals/goal-service.ts              — Goal 서비스 레이어
[NEW] src/features/goals/hooks/useWeeklyGoals.ts      — Goal React 훅
[NEW] src/features/goals/components/GoalSettingsDialog.tsx    — 목표 설정 다이얼로그
[NEW] src/features/goals/components/GoalSettingsDialog.module.css — 다이얼로그 스타일
[NEW] tests/dates.test.mjs                            — weekStartAtMs 유틸리티 테스트
[NEW] tests/goal-service.test.mjs                     — goal-service 로직 테스트
[MODIFY] src/features/topics/components/TopicCard.tsx  — 목표 설정 버튼 + 목표 표시 추가
[MODIFY] src/features/topics/components/TopicCard.module.css — goalBadge 스타일 추가
[MODIFY] src/features/topics/TopicsPage.tsx            — useWeeklyGoals 훅 연동
```

### 명시적으로 수정하지 말아야 할 영역

- `src/domain/goals/` — Story 4.1에서 완성됨. 타입/스키마/매퍼/repository 수정 불필요
- `src/domain/topics/` — topic 도메인 수정 불필요
- `src/domain/sessions/` — session 도메인 수정 불필요
- `src/db/migrations/` — 새 마이그레이션 불필요 (테이블/인덱스 이미 존재)
- `src/features/session/` — 세션 관련 기능 수정 불필요
- `src/shared/lib/result.ts`, `errors.ts` — 기존 유틸 재사용만
- `src/platform/tauri/sql-client.ts` — 기존 adapter 재사용만

### Anti-Patterns to Avoid

- ❌ `domain/goals/` 파일 수정 — 도메인은 Story 4.1에서 완성됨
- ❌ 새 마이그레이션 파일 생성 — DB 스키마 이미 존재
- ❌ `GoalSettingsDialog` 안에서 repository 직접 호출 — 반드시 service → adapter → repository 경로 사용
- ❌ `TopicCard` 안에서 `findByTopicAndWeek` 직접 호출 — 훅/서비스를 통해 접근
- ❌ Zustand store에 goal 데이터 캐시 — 이 스토리에서는 `useWeeklyGoals` 로컬 상태로 충분
- ❌ `weekStartAtMs` 계산을 컴포넌트/서비스에서 인라인으로 수행 — 반드시 `shared/lib/dates.ts` 유틸리티 사용
- ❌ Dialog를 shared/ui로 바로 추출 — 아직 2개 이상 feature에서 사용하지 않음. `features/goals/components/` 안에 로컬로 구현
- ❌ `type="text"` 입력 필드에 수동 숫자 파싱 — `type="number"` 사용, 서버 측은 Zod로 검증
- ❌ `tokens.css` 변수 대신 하드코딩 색상/간격 사용
- ❌ `topic-adapter.ts` 패턴을 무시하고 다른 adapter 구조 사용

### Previous Story Intelligence

**Story 4.1에서 확인된 패턴:**

- `weekly-goal-repository.ts` 구현 완료 — `createWeeklyGoal`, `updateWeeklyGoal`, `findByTopicAndWeek`, `findAllByWeek` 4개 함수
- `findByTopicAndWeek`이 결과 없으면 `ok(null)` 반환 — 목표 미설정 상태를 정상으로 처리
- `createWeeklyGoal`이 중복 체크(`topic_id + week_start_at_ms`) 수행 — 동일 주제+주에 대해 중복 생성 방지됨
- `UpdateWeeklyGoalSchema`는 `targetMinutes` 하나만 검증 — `z.number().int().min(1, '목표 시간은 1분 이상이어야 합니다')`
- 테스트는 `tests/` 디렉터리에 `.test.mjs` 확장자, `node --test` 러너 사용
- `tsconfig.tests.json`에 도메인 파일 3개 추가됨 — 이 스토리에서도 새 파일 추가 시 `tsconfig.tests.json` 업데이트 필요할 수 있음

**기존 UI 패턴에서 확인된 사항:**

- `TopicForm.tsx` — Zod `safeParse` → 에러 표시 → `isSaving` → 결과 처리 패턴 확립
- `TopicCard.tsx` — 인라인 수정 모드(isEditing), 아카이브 확인 모드(showArchiveConfirm) 상태 전환 패턴 확립
- `Button` 컴포넌트 — `variant`, `size`, `isLoading`, `disabled` props 사용
- `topic-service.ts` — `platform/browser/topic-adapter` 통해 repository 접근 (런타임 분기)
- `useTopics.ts` — `useState + useEffect + useCallback` 패턴. `fetchTopics` → 결과 분기 → setTopics/setError
- `cn()` 유틸리티 — 조건부 className 결합에 사용 (`shared/lib/cn.ts`)
- `TopicCard.module.css` — `--color-*`, `--space-*` CSS 변수 활용

### Git Intelligence

최근 커밋:
- `66a8749` — Story 3.6: session recovery variant 구현
- `b17db3b` — Zustand 기반 세션 상태 관리
- `991b60d` — 브라우저 QA mode + in-memory adapter

시사점:
- Story 4.1 구현 결과가 커밋에 아직 반영되지 않았을 수 있음 (로컬에는 존재)
- in-memory adapter 패턴은 `in-memory-topic-adapter.ts`, `in-memory-session-adapter.ts`에서 확립됨
- `isTauriRuntime()` 함수로 런타임 분기하는 패턴이 `topic-adapter.ts`, `session-adapter.ts`에서 사용됨

### Project Structure Notes

- `src/features/goals/` 디렉터리 존재하며 `.gitkeep`만 있음 — 이 스토리에서 `goal-service.ts`, `hooks/`, `components/` 생성
- Architecture 문서의 `src/features/goals/` 구조 (line 659-665): `GoalSettingsDialog.tsx`, `GoalProgressInline.tsx`, `hooks/useWeeklyGoals.ts`, `goal-service.ts`
- `GoalProgressInline.tsx`는 Story 4.3 범위 — 이 스토리에서는 구현하지 않음
- `src/shared/ui/Dialog/` — Architecture 문서에 명시되어 있으나 아직 존재하지 않음. 이 스토리에서는 로컬 다이얼로그로 구현

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 4.2]
- [Source: _bmad-output/planning-artifacts/epics.md#Epic 4]
- [Source: _bmad-output/planning-artifacts/architecture.md#Frontend Architecture]
- [Source: _bmad-output/planning-artifacts/architecture.md#Project Structure & Boundaries, line 659-665]
- [Source: _bmad-output/planning-artifacts/architecture.md#Implementation Patterns & Consistency Rules]
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md#Form Patterns]
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md#UX-DR14 폼 패턴 일관성]
- [Source: _bmad-output/planning-artifacts/prd.md#FR14, FR33]
- [Source: src/features/topics/topic-service.ts — 서비스 레이어 패턴]
- [Source: src/platform/browser/topic-adapter.ts — adapter 런타임 분기 패턴]
- [Source: src/features/topics/hooks/useTopics.ts — 훅 패턴]
- [Source: src/features/topics/components/TopicForm.tsx — Zod 클라이언트 검증 + 폼 패턴]
- [Source: src/features/topics/components/TopicCard.tsx — 인라인 UI 상태 전환 패턴]
- [Source: src/domain/goals/weekly-goal-repository.ts — repository CRUD 함수]
- [Source: src/domain/goals/weekly-goal-schema.ts — UpdateWeeklyGoalSchema]
- [Source: _bmad-output/implementation-artifacts/4-1-주간-목표-도메인-모델-및-저장소-구현.md — 이전 스토리]

## Dev Agent Record

### Agent Model Used

Claude (Antigravity)

### Debug Log References

### Completion Notes List

- ✅ Task 1: `getWeekStartAtMs()` 유틸리티 구현 — ISO 8601 기준 월요일 00:00:00 UTC 반환, 11개 테스트 통과
- ✅ Task 2: Goal adapter 레이어 — `topic-adapter.ts` 패턴 따라 `goal-adapter.ts` + `in-memory-goal-adapter.ts` 구현
- ✅ Task 3: `goal-service.ts` — `loadGoalForTopicThisWeek`, `saveGoal` (create/update 분기), `loadAllGoalsThisWeek` 구현
- ✅ Task 4: `useWeeklyGoals` 훅 — `Map<topicId, WeeklyGoal | null>` 상태 관리, `saveGoalForTopic`/`loadGoalForTopic`/`refetch` 노출
- ✅ Task 5: `GoalSettingsDialog` — 모달 오버레이, Zod 검증, ESC/오버레이 닫기, 포커스 트랩, aria 접근성 제공
- ✅ Task 6: TopicCard 통합 — `weeklyGoal` prop 추가, 목표 배지 표시, "목표 설정/수정" 버튼, `TopicList`/`TopicsPage` 훅 연동
- ✅ Task 7: 테스트 20개 작성 (dates 11개 + goal-service 9개), 모두 통과
- ✅ Task 8: `npx tsc --noEmit` 통과, `npm run build` 성공 (196 modules), 기존 테스트 회귀 없음 (52/53, 1건 기존 이슈)

### File List

- [NEW] src/shared/lib/dates.ts
- [NEW] src/platform/browser/goal-adapter.ts
- [NEW] src/platform/browser/in-memory-goal-adapter.ts
- [NEW] src/features/goals/goal-service.ts
- [NEW] src/features/goals/hooks/useWeeklyGoals.ts
- [NEW] src/features/goals/components/GoalSettingsDialog.tsx
- [NEW] src/features/goals/components/GoalSettingsDialog.module.css
- [NEW] tests/dates.test.mjs
- [NEW] tests/goal-service.test.mjs
- [MODIFY] src/features/topics/components/TopicCard.tsx
- [MODIFY] src/features/topics/components/TopicCard.module.css
- [MODIFY] src/features/topics/components/TopicList.tsx
- [MODIFY] src/features/topics/TopicsPage.tsx
- [MODIFY] tsconfig.tests.json

### Change Log

- 주간 목표 설정 UI 전체 구현 — dates 유틸리티, adapter 레이어, 서비스, 훅, 다이얼로그, TopicCard 통합 (Date: 2026-03-19)
